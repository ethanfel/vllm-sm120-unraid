# The target model is public, so HF_TOKEN is optional. It is useful for private
# models and to avoid anonymous Hub rate limits.
HF_TOKEN=

# Leave empty for an unauthenticated LAN endpoint. If set, use the same value in
# the ComfyUI/OpenAI-compatible client.
API_KEY=

MODEL_ID=sakamakismile/Qwen3.8-27B-AEON-ULTIMATE-UNCENSORED-NVFP4
SERVED_MODEL_NAME=qwen3.8-27b-aeon-nvfp4

# Single-RTX-PRO-6000 NVFP4 profile using the model's native context.
MAX_MODEL_LEN=262144
GPU_MEMORY_UTILIZATION=0.90
MAX_NUM_SEQS=4
MAX_NUM_BATCHED_TOKENS=8192

# Fully unload vLLM after ten idle minutes, and do not launch unless enough
# VRAM is free. Idle ComfyUI caches can be released automatically.
ENABLE_ON_DEMAND=true
IDLE_TIMEOUT_SECONDS=600
MODEL_LOAD_TIMEOUT_SECONDS=1200
MODEL_STOP_TIMEOUT_SECONDS=120
MIN_FREE_VRAM_MIB=90000
COMFYUI_BASE_URL=http://host.docker.internal:8188
COMFYUI_FREE_TIMEOUT_SECONDS=120
VLLM_INTERNAL_PORT=8001

# The dashboard can list cached Hugging Face models and remove unused ones.
# Set false to retain disk-usage visibility while disabling deletion.
MODEL_CACHE_DELETE_ENABLED=true

ENABLE_PREFIX_CACHING=true
ENABLE_TOOLS=true
TOOL_CALL_PARSER=qwen3_coder
ENABLE_MTP=true
# Default: {"method":"mtp","num_speculative_tokens":3}
LANGUAGE_MODEL_ONLY=false
LIMIT_MM_PER_PROMPT={"image":4,"video":0}
DEFAULT_CHAT_TEMPLATE_KWARGS={"enable_thinking":true,"reasoning_effort":"medium","preserve_thinking":true}

# FP8 halves KV-cache memory on Blackwell. Set bfloat16 for diagnosis if needed.
KV_CACHE_DTYPE=fp8
